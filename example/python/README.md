# VectorBlox SDK Python Simulation

This directory contains Python post-processing scripts for simulation. Modify these scripts as needed for your own models.

## Pre-requisites

- Set up the `VBX_SDK` environment (see steps [here](../../README.md)).
- Generate a `.vnnx` network from the [tutorials directory](../../tutorials/).
